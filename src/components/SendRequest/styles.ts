import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  buttons: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    // justifyContent: 'center'
    marginTop: 10,
  },
  sendRequest: {
    width: '100%',
    flex: 1,
    marginRight: 10
  }
})