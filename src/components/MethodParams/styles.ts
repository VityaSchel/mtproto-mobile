import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  params: {
    display: 'flex',
    width: '100%',
    marginTop: 20,
  },
  param: {
    display: 'flex',
    flexDirection: 'row',
    width: '100%'
  }
})