import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  view: {
    display: 'flex',
    flex: 1,
    width: '100%',
    height: '100%',
    paddingTop: 50,
  },
  subTypeSelector: {
    width: '100%',
    display: 'flex',
    borderWidth: 1,
    borderColor: 'blue'
  },
  subTypeSelectorItem: {
    
  }
})