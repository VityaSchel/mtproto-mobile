import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  title: {
    fontWeight: 'bold',
    marginBottom: 20
  },
  input: {
    width: '100%',
    marginBottom: 20
  },
  rowFlex: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 20
  }
})